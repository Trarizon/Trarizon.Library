<Project Sdk="Microsoft.NET.Sdk">

    <PropertyGroup>
        <TargetFramework>netstandard2.0</TargetFramework>
    </PropertyGroup>

    <Import Project="..\..\SourceGenerator.props" />

    <!-- References -->

    <ItemGroup>
        <PackageReference Include="PolySharp">
            <PrivateAssets>all</PrivateAssets>
            <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
        </PackageReference>
        <PackageReference Include="Trarizon.Library.Functional" PrivateAssets="all" GeneratePathProperty="true" />
        <PackageReference Include="Trarizon.Library.Linq" PrivateAssets="all" GeneratePathProperty="true" />
        <PackageReference Include="System.Memory" PrivateAssets="all" GeneratePathProperty="true" />
        <PackageReference Include="Trarizon.Library.Roslyn" PrivateAssets="all" GeneratePathProperty="true" />
    </ItemGroup>

    <!-- <ItemGroup>
        <ProjectReference Include="..\Trarizon.Library.Roslyn\Trarizon.Library.Roslyn.csproj" />
    </ItemGroup> -->

    <!--Dependency-->

    <ItemGroup>
        <_GeneratorDependencyDlls Include="$(PKGTrarizon_Library_Functional)\lib\netstandard2.0\*.dll" />
        <_GeneratorDependencyDlls Include="$(PKGTrarizon_Library_Linq)\lib\netstandard2.0\*.dll" />
        <_GeneratorDependencyDlls Include="$(PKGTrarizon_Library_Roslyn)\lib\netstandard2.0\*.dll" />
        <_GeneratorDependencyDlls Include="$(PKGSystem_Memory)\lib\netstandard2.0\*.dll" />
    </ItemGroup>

    <PropertyGroup>
        <GetTargetPathDependsOn>$(GetTargetPathDependsOn);GetDependencyTargetPaths</GetTargetPathDependsOn>
    </PropertyGroup>

    <Target Name="GetDependencyTargetPaths">
        <ItemGroup>
            <TargetPathWithTargetPlatformMoniker Include="@(_GeneratorDependencyDlls)" IncludeRuntimeDependency="false" />
        </ItemGroup>
    </Target>

    <Target Name="CopyDependenciesToOutput" AfterTargets="Build">
        <Copy SourceFiles="@(_GeneratorDependencyDlls)" DestinationFolder="$(OutputPath)" SkipUnchangedFiles="true" />
    </Target>

    <!--End Dependency-->

</Project>